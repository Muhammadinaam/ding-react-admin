/**
 * Resolve `labelSource` to an absolute RHF path.
 * A single segment (no dots) is a sibling of the field `name` (or `source`).
 */
export declare function resolveLabelSourcePath(labelSource: string, fieldName: string | undefined, source: string): string;
//# sourceMappingURL=resolveLabelSourcePath.d.ts.map